@model IEnumerable<MDC.Models.TipologiaPratica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descrizione)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descrizione)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTipologiaPratica }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTipologiaPratica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTipologiaPratica })
        </td>
    </tr>
}

</table>
